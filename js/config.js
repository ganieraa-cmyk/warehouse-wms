/**
 * config.js — Global constants shared across all modules.
 * Must be loaded FIRST in index.html before all other scripts.
 */
const MAIN_BINS = ['A', 'B', 'C', 'D', 'E'];
const SUB_COUNT = 5;
const CAP       = 100;
